---
name: jooq-rules
description: jOOQ 로 쿼리를 작성·리뷰할 때 적용하는 규약. 코드생성 메타모델, 프로젝션 우선(selectFrom 은 전부 필요할 때만), Record→도메인 매핑 경계, cursor 페이지네이션과 서명, 동적 조건 조립, N+1 대응(batch fetch / MULTISET), 대량 쓰기와 chunk 근거, 트랜잭션 경계와 Outbox, 컴파일이 못 잡는 것의 테스트 전략. jOOQ 를 ORM 이 아니라 타입세이프 SQL DSL 로 쓰기 위한 룰.
---

# jOOQ 사용 규약

> jOOQ 는 **타입 세이프 SQL DSL** 이다. ORM 이 아니다.
> SQL 을 숨기는 도구가 아니라, **SQL 을 컴파일 타임에 검증 가능하게 만드는** 도구다.

이 구분이 모든 규칙의 뿌리다. "SQL 이 안 보이게" 만드는 추상화는 jOOQ 를 쓰는 이유를 지운다.

**이 문서는 규칙과 판별 기준이다.** 근거·코드 예제·상세 절차는 `references/` 에 있고, 판단이 필요할 때 연다.

| 참조 | 언제 읽나 |
|---|---|
| `references/queries.md` | 기준 버전·codegen 설정, 프로젝션, 널 처리, 매핑, 동적 쿼리 |
| `references/pagination.md` | cursor 설계, tie-breaker, **HMAC 서명**, COUNT |
| `references/writes.md` | N+1, **대량 쓰기**, 트랜잭션 경계와 Outbox |
| `references/testing.md` | 컴파일이 못 잡는 것을 어떻게 테스트로 잡는가 |

예제 도메인은 커머스(`ORDERS` / `ORDER_ITEMS` / `PRODUCTS`)다. 실제 테이블·정책은 해당 리포에서 가져온다.

---

## 1. 왜 jOOQ 인가

| | JPA / Hibernate | jOOQ |
|---|---|---|
| 접근 | 객체 중심. SQL 을 숨긴다 | SQL 중심. SQL 이 코드에 보인다 |
| 쿼리 예측 | N+1 이 언제 나는지 모른다 | 실행될 쿼리가 코드에 명시적 |
| 스키마 변경 | 런타임에 터진다 | codegen → **컴파일 에러**로 먼저 안다 |
| 실수의 발견 시점 | 배포 후 그 쿼리가 실행될 때 | 빌드 시점 |

JPA 는 단순 CRUD 에 좋다. **쿼리 모양을 통제해야 하는 영역**에서는 jOOQ 가 다루기 쉽다.

---

## 2. 코드 생성 메타모델

- **스키마 식별자(테이블·컬럼)는 생성 클래스로 참조한다.** 문자열은 오타를 런타임까지 끌고 간다
- 스키마 변경 절차: 마이그레이션 → codegen → **컴파일**. 3번째를 건너뛰면 codegen 의 의미가 없다 — **컴파일이 곧 영향도 분석이다**
- **예외:** `ts_rank`, JSON 연산자처럼 codegen 이 표현 못 하는 **표현식**은 plain SQL 이 정당하다. 단 바인드 변수로 넘기고 한 곳에 격리한다
- 생성 옵션은 `isPojos = false` / `isDaos = false`. 생성 DAO 를 쓰는 순간 프로젝션도 조건도 통제를 잃는다

→ 기준 버전과 codegen 설정 스니펫: `references/queries.md` §1~2

---

## 3. 프로젝션

**프로젝션은 성능 최적화이기 이전에 계약이다.** 규칙은 "`selectFrom` 을 쓰지 마라"가 아니라 **"이 쿼리가 무엇을 읽는지 의도가 드러나게 하라"** 다.

> **판별 기준: "이 쿼리가 읽고도 안 쓰는 컬럼이 있는가?"**
> 있으면 프로젝션. 없으면 `selectFrom` 을 써도 된다.

- 목록과 상세는 프로젝션이 다르다. 하나의 만능 조회를 만들지 않는다
- **집계·조인 결과를 도메인 엔티티에 nullable 필드로 끼워넣지 않는다.** 채워진 경로와 아닌 경로를 호출처가 구분할 수 없으면 **타입이 거짓말을 하는 것**이다 → 전용 프로젝션 타입
- 남는 트레이드오프: 지금 전부 필요해도 **나중에 추가되는 컬럼은 자동으로 딸려온다.** 넓어질 테이블·목록 경로는 프로젝션이 안전하다

---

## 4. 쿼리 스타일

- select → from → where → orderBy → limit. **읽으면 SQL 이 보여야 한다**
- 조건이 길어지면 의미 있는 변수로 뽑는다. 변수명이 곧 도메인 규칙의 문서다

---

## 5. 타입 안전성과 널 처리

**Adapter 는 DB 가 말한 것을 그대로 옮긴다. 해석은 도메인이 한다.**

Adapter 에서 `?: ""` 로 NULL 을 메우면 "아직 안 왔다"와 "실패했다"가 하나로 뭉개진다. 두 상태를 구분해야 하는 화면은 그때부터 분기할 수 없다.

---

## 6. 매핑 경계

- jOOQ 는 infrastructure(adapter) 레이어에서만 쓴다
- Port 반환 타입은 **언제나 도메인 모델**. 시그니처에 `Record` 가 보이면 경계가 이미 깨진 것이다
- **Record → 도메인 매퍼는 private.** public 이면 변환이 여기저기 생기고, 필드 하나 추가할 때 찾아다녀야 한다

---

## 7. 페이지네이션

**쌓이는 목록의 기본값은 cursor.** OFFSET 은 "N 행을 건너뛰라"가 아니라 **"N 행을 읽고 버려라"** 다.

> **cursor 의 대가: 앞뒤 순차 이동만 된다.**
> "N 페이지로 점프"와 "전체 몇 페이지"를 포기하는 것이 선택의 트레이드오프다.
> **UI 요구사항을 먼저 확인하고 고른다** — 페이지 번호가 박힌 관리자 테이블이면 OFFSET 이 맞다.

- 정렬 키 값을 **전부** cursor 에 봉인한다. 하나라도 빠지면 페이지 경계에서 행이 새거나 겹친다
- tie-breaker 는 단조 증가 id
- **외부에 노출하는 cursor 는 HMAC 서명한다.** 호출자 식별자와 필터 해시를 함께 봉인하고 복호화 시 대조한다. 안 하면 값을 고쳐 다른 범위를 긁을 수 있다
- COUNT 는 별도 쿼리. 비싸면 총계를 포기하는 것도 설계다

→ 서명 구현과 흔한 실수 넷: `references/pagination.md` §4

---

## 8. 동적 쿼리와 재사용

**허용:** 조건 조립, 프로젝션 상수, 매퍼 — SQL 구조가 보이는 것
**금지:** 쿼리 모양 자체를 감추는 것 (jOOQ 위의 ORM)

> **판별 기준: "이 함수를 읽고 실행될 SQL 을 그릴 수 있는가?"** 못 그리면 과한 추상화다.

---

## 9. N+1 방지

**기본은 batch fetch** — 쿼리 수가 데이터 건수와 무관하게 상수로 고정된다.

`MULTISET` 은 쿼리 1회 + 타입 안전이라는 장점이 분명하지만, **왕복 비용이 실제로 측정된 뒤에** 전환한다. "한 방 쿼리가 멋있어서"는 도입 근거가 아니다.

---

## 10. 대량 쓰기

루프 안 `execute()` 는 **쓰기 쪽 N+1** 이다. 기본은 다중 행 INSERT.

- **chunk 크기에는 근거가 있어야 한다.** PostgreSQL 바인드 파라미터 상한은 문장당 65535 개 — `행 수 × 컬럼 수` 로 역산한다. "적당히 1000개"는 근거가 아니다
- **전부를 한 트랜잭션에 넣지 않는다.** 락 유지 시간과 롤백 비용이 작업 시간에 비례한다
- 단 chunk 커밋은 **부분 성공 상태**를 만든다 → **멱등성(upsert)이 전제**다. 배치는 반드시 다시 돌게 된다
- upsert 에는 "늦게 도착한 오래된 이벤트가 최신 값을 덮지 않게" 하는 조건을 건다

→ `references/writes.md` §2

---

## 11. 트랜잭션 경계와 Outbox

**외부 호출은 트랜잭션 밖에서 한다.** 외부 API 가 느려지면 DB 커넥션이 그 시간만큼 잡혀 있고 결국 풀이 마른다. 트랜잭션 안에서는 **의도만 기록**(outbox)하고, 발행은 워커가 한다.

`@Transactional` 은 UseCase 레이어에만. Adapter 에 붙이면 경계가 흐려진다.

---

## 12. 테스트

이 규약의 근거가 "컴파일 타임 검증"이므로, **컴파일이 못 잡는 것이 정확히 테스트의 범위다.**

| 컴파일이 잡는다 | 테스트가 잡아야 한다 |
|---|---|
| 컬럼명·타입·존재 여부 | 조인 카디널리티, 조건 논리, soft delete 필터 누락, cursor 경계, 멱등성, 쿼리 개수 |

- **실제 DB(Testcontainers)로 돌린다.** 인메모리 대체는 방언이 달라 보증이 없다
- **`DSLContext` 를 mock 하지 않는다.** mock 하면 SQL 이 맞는지가 검증에서 통째로 빠진다
- 마이그레이션을 테스트에서도 그대로 실행한다 — 스키마 진실을 하나로 유지
- N+1 은 기능 테스트를 전부 통과한다. **쿼리 수 자체를 단언**한다

→ 다섯 가지 필수 케이스와 코드: `references/testing.md`

---

## 13. 새 Adapter 체크리스트

```
□ 스키마 식별자는 생성 메타모델 — plain SQL 은 표현식에만, 바인드 변수로, 한 곳에 격리
□ 프로젝션 명시가 기본 — selectFrom 을 썼다면 모든 컬럼이 실제로 필요한 경우인가
□ 유스케이스별 프로젝션 분리 (목록 ≠ 상세)
□ 집계·조인 결과는 전용 프로젝션 타입으로 (엔티티에 nullable 통계 필드 금지)
□ Record → 도메인 매퍼가 private
□ Port 반환 타입이 도메인 모델 — Record 노출 없음
□ nullable 디폴트를 Adapter 에서 넣지 않음
□ SQL 순서가 자연스럽고, 복잡한 조건은 의미 있는 변수로 분리
□ 페이지네이션 방식이 UI 요구와 맞는가 (쌓이는 목록→cursor / 페이지 점프 필요→OFFSET)
□ 외부 노출 cursor 는 서명 + 호출자·필터 봉인
□ N+1 위험 시 batch fetch 기본, MULTISET 은 측정 후
□ 대량 쓰기의 chunk 크기에 근거가 있고, 재실행이 멱등한가
□ 외부 호출은 트랜잭션 밖 — 안에서는 outbox INSERT 만
□ soft delete 테이블은 deleted_at IS NULL 필터 확인
□ 테스트가 실제 DB 로 돌고, 쿼리 수를 단언하는가
```
